# Sugata recovery — September 22, 2026

Robert authorized further development, tests and full git operations. The checkout started
clean at `afad2fb` on `main`, matching the fetched `origin/main`. The detached `affc` worktree
is untouched. The older automation and separate Goal remain outside this session.

## Reproducible collar investigation

The September 16 checkpoint depended on ignored `captures/` files that were absent on this
checkout. Its garment recipes and key rendered witness survived in Git. The accepted garment
and experimental collar still reconstruct to their recorded hashes. Capture now reconstructs
and saves those inputs itself, imports tracked browser/snapshot helpers, freezes its instruments,
and records hashes for runtime sources and assets. The numerical reviewer accepts a fresh
output directory and reads the exact assets saved with a version 2 capture.

```sh
node tools/figure-pipeline/collar_stress_capture.mjs captures/collar-stress-NEW/gpu
node tools/figure-pipeline/collar_stress_review.mjs captures/collar-stress-NEW/gpu captures/collar-stress-NEW/cpu
node tools/figure-pipeline/collar_fit_support.mjs /tmp/collar-support-NEW.json
```

Use new output paths. The capture needs hydrated repository assets and the Chromium/Playwright
setup used by `MotionProbe.mjs`; it needs no September 13/16 archive. It routes reconstructed
GLBs through the actual loader without replacing the installed garment. The review retains
legacy capture compatibility, but a new capture must carry its own frozen instruments: a
matching working-tree file cannot substitute for a missing saved helper.

The [original fit plan](COLLAR-FIT-PLAN-2026-09-16.md) was recovered from the recorded session,
with its exact SHA-256 `e7fd813ab5b7b5298248c5b7f641905cb99c166baabf9591225e22e8b3f18ba0`.
Its references and commands are historical. Use `collar_fit_support.mjs` for the now-portable
topology inspection. The [new support record](evidence/collar-fit-support-2026-09-22.json)
reconstructs 34 welded moving groups, 43 raw vertices and 91 incident faces, including the
10 faces outside the interior band. Moving seam duplicates have identical joints/weights;
the rear guard is disjoint. These are proposed solver bounds, not a new fit or a clearance pass.

## Fresh evidence

The [recovery evidence](evidence/collar-recovery-2026-09-22.json) and its
[manifest](evidence/collar-recovery-2026-09-22.manifest.json) bind the completed run:

- Four 18-second GPU traces: previous versus experimental garment, each with bob01 and bob02.
  They contain 1,444 portrait images, 12 full-body images and 76 geometry snapshots. Paired
  physical state, rig, morphs, telemetry and renderer clocks match exactly. All runs report
  no browser errors, zero tracked resources after disposal and no leaked handles.
- The experimental bob02 frame at step 99 reproduces the tracked September 16 ordinary
  witness **byte for byte**, SHA-256 `66a3e540658b7ed11e8662b76dcb1d7f7b9e424ba84aa37f9b402f1df140f4a5`.
- Numerical review reproduces bra crossings at step 98 on faces 1327/1328/1329/1331 and at
  step 270 on 544/545/1328/1329/1331, for both bobs. The minimum body gap is again
  **0.04685342446242559 mm**, garment face 1515 versus body 15580 at step 720.
  The candidate has no body crossing or inside/ambiguous classification on the reviewed patch.
- All 12 analytic geometry controls pass. The review covers 19 distinct poses per garment,
  using CPU-skinned snapshots taken during GPU rendering. It is not a continuous-motion
  certificate or whole-avatar clearance claim.

Three selected stills were directly viewed: the previous/experimental step-99 pair and the
experimental bob01 angry full-body view. The latter still shows hands entering trousers and
broken-up hair. There is no new independent visual acceptance or claim of exhaustive viewing.

All recorded shared avatar/Showcase rendering code and asset hashes still match after the work.
Five recorded files changed after capture: the three hair test/probe files and the separate
Converse page and its browser test, repaired below. Raw evidence remains local under
`captures/collar-stress-2026-09-22/`; the 1,629-file manifest, compact result and reproduction
tools are tracked. The original deleted archive has not been recreated or represented as recovered.

## Test repair

The two Avatar hair GPU gates expected 35 storage buffers after beauty rendering. The current
runtime also owns one committed-card history buffer: 16,864 vertices × 16 padded bytes =
269,824 bytes. The probes now retire that history owner alone and verify the measured change
from 36 to 35 buffers, with exactly that byte reduction and a still-live solver. The existing
35/8/0 solver-only rebuild checks and zero-resource final retirement remain unchanged.
Both gates pass all nine groups, including their unchanged canonical GPU geometry goldens.
See [the retained reports](evidence/hair-history-gates-2026-09-22.json).

The new input tests reject overwritten, missing, altered or escaped assets and missing/altered
frozen instruments. The support test reproduces the saved lists and rejects mismatched seam
weights, joints or vertex topology. Collar/trouser reproduction, wardrobe's 51 assertions,
document claims and the 18-page build pass. The build retains its existing large-chunk warning.

The broader suite also exposed currently failing hair-shadow area and wardrobe-shadow acne
checks that were absent from the active red-gate list. They are now declared with the observed
measurements in [RED-GATES.md](RED-GATES.md). The hair material recheck is 75/80, with its
secondary-band attribution clause failing as well as the four previously documented appearance
clauses. These rendering failures are on unchanged runtime/assets; no thresholds were relaxed.

## Converse startup and full-suite results

Converse's default page failed before model discovery: it constructed an incomplete selection
for `optionsForShowcase`, omitting the now-required wardrobe style. It now obtains a complete
selection from the shared resolver, retaining the Everyday preset and its Ecru colours. The
actual browser test verifies that attached appearance, then passes **32 groups**, including
connection, replies, failure diagnostics, retries, mobile layout and own-clock retirement.
Desktop and mobile screenshots were directly inspected. Model discovery/completions used
controlled fixtures; no real inference, service restart or model change occurred. See
[the startup evidence](evidence/converse-startup-2026-09-22.json).

The [complete repository run](evidence/selftests-2026-09-22.txt) ran **121 gates**: **107 passed,
14 failed**. It began clean at `afad2fb` and ended with the working tree dirty during the repairs;
this is a dated run, not a claim of a clean final-tree sweep. Focused reruns then cleared three
failures: the two hair lifetime gates and Converse. **Eleven failures remain**, all declared in
[RED-GATES.md](RED-GATES.md). The two newly added recovery/support tests also pass. The full
run exited 17: 14 failed gates plus three undeclared failures, exactly the three subsequently
repaired gates. Its stale-declaration count was zero. No test failure was suppressed.

Remaining areas are hair appearance/dynamics/alpha/geometry, sway balance, hair/wardrobe shadows,
the intermittent OIT exact-zero check, two unclassified Stage subjects in the toggle instrument,
reference-dependent quoted claims, and the stale request ledger. Current raw rechecks are linked
from the failure ledger; older counts in its historical prose are explicitly superseded.

The runner now saves complete stdout/stderr for every gate in a fresh temporary log directory,
prints that directory at startup and a direct log path for each failure. This fixes the observed
loss of an early assertion outside its 20-line tail. `bash -n` and a controlled invocation of
the actual `run_gate` function verify both pass/fail logs and unchanged failure accounting;
see [the control output](evidence/selftest-log-control-2026-09-22.txt). The entire long suite
was not repeated solely for this logging change.

## Next work

The accepted asset remains the default; the experimental collar is still unpromoted.
Continue with one bounded, cloth-only fit using the recovered support and both body and
retained-bra obstacles. Preserve the 3 mm displacement cap and all shape/coverage/motion gates
in the plan. Historical mild-pose and coverage replay helpers also referenced ignored files;
recover or replace those explicitly before claiming a new asset is qualified.

The restored capture path and support inspection are development progress. They do not resolve
the collar contact, hair appearance, fragile rear clearance or the broader AAA character target.
