# g025 long-bob contact calibration

The body-contact registry now recognizes the exact portable g025 rest composition
`556f67a72189082a1b19c5d3fa9f4f3c8860a660cb4ba085e9c990b651389716` on its measured
figure_g025 body. The shipping groom is unchanged. Its original geometry cannot activate the
corrected-rest calibration. The 47-pose runtime diagnostic is complete and remains unaccepted;
this record does not promote the groom.

The registry preserves the existing g050 exported record. Selection hashes normalized component
values, canonical unmasked body indices, bone names and inverse binds for the actual chosen bake.
The owner accepts only a registered record; the Avatar gate retains every post-await retirement
check. The promoted exact-input query cache, solver and material behavior are unchanged.

The g025 patch is derived from the 861 corresponding support vertices in the tracked anatomy
calibration: 1,872 body triangles and 1,069 corners. Its own positions, normals, indices and inverse
binds have separate fingerprints. All 496 fixed roots and first-span norm tubes clear the authored
patch and one available actual posed0 configuration. Minimum root clearance is 27.712/26.556 mm;
minimum first-span clearance is 20.633/18.638 mm, including the current 0.1 mm margin. The two
earlier arm captures contain the same posed0 geometry and are not independent stress poses.

All 251 remaining authored root-layer crossings on cards 2, 3, 50 and 66 lie outside this patch.
They remain explicit; adding neck contact does not resolve higher scalp geometry or prove its
visibility. Full-ribbon/body replay and natural/held bidirectional nod diagnostics are now complete.
The failures below, broader stress motion, appearance and actual frame cost remain acceptance gates.

CPU validation passes 16 calibration/owner groups, 137 Avatar checks and four actual deferred
digest retirement cases covering both bakes. The actual Avatar smoke routes only the exact groom GLB into the real path. It retains actual
GPU positions and velocities, skin/morph cap positions, full body positions/normals, rig/camera
state and source-response provenance.
Orientation views use draw-only rendering, restore the camera exactly, and assert that simulation
and body snapshots do not change. No renderer or solver method is replaced.

See `docs/evidence/bob01-g025-contact-registry-2026-09-09.json` for exact source hashes and the
archived calibration proof and smoke provenance.

The two-pose smoke at 0 and 0.1 seconds activates the expected calibration and exact-input cache
with zero runtime errors. All eight orientation images are nonblank; full geometry, velocity,
body, rig, clock and camera snapshots remain exact across the views. Complete-hair snapshots
contain 17,516 vertices; only the 16,864 card vertices are GPU readbacks. The 652 non-card
vertices use renderer-equivalent CPU skin/morph reconstruction.

The strict geometric face gate changes from zero to five pairs on card 44's last two spans.
Neck/shoulder and movable-curtain pairs stay zero. The centers remain 14.737 and 15.601 mm
outside the body; intended width transport rotates the terminal frames by 7.48 and 10.42 degrees,
consuming a small inward-edge margin. Rigid rest hair against the posed body has zero pairs,
while actual hair against the rest body reproduces the five pairs. The attachment and upper
prefix through ring 11 remain unchanged from the original root-support repair.

Every crossing segment's entire UV bounding rectangle plus a one-texel halo is zero-alpha in
the base atlas (436 rectangle-texel checks, including repeated-border neighbors). This establishes
transparent nearest/bilinear mip-zero support at the crossings, not final mip-filtered visibility.
The geometric face gate remains failed. Visibility qualification subsequently allowed diagnostic
natural/nod observation to continue; it did not justify a shape correction or acceptance.

The original smoke's eight synchronous draw calls share a renderer epoch and leave conspicuous
stipple, so these plates are not converged appearance acceptance. Its exact harness is preserved.
The revised harness awaits GPU/compositor completion after each draw while retaining the exact
snapshot assertions. It passed all 47 subsequent g025 poses and 36 orientation views with zero
errors. All original smoke sources, the routed groom, measurements, ten images and original/next
harnesses are archived under
`captures/bob01-g025-avatar-smoke-2026-09-09` with a verified 120-file manifest.

The separate card 44 texture probe sampled the actual allocated GPU atlas with its actual sampler.
CPU footprint estimates used the recorded 0.66 scene resolution. Seventy-two explicit-LOD queries
were finite, with maximum CPU/GPU alpha difference 0.00222; four clipped front witnesses had
alpha 0.0267–0.1838. No half-opaque clipped witness was found in that bounded neighborhood.
These are explicit-LOD samples, not raster-derivative or final stochastic/TAAU visibility parity.
The two failed pre-draw texture lookups and superseded display-resolution estimate remain in
`captures/bob01-g025-alpha-footprint-2026-09-09` with the corrected probe and a 14-file manifest.

The complete motion diagnostic contains 13 natural poses over 12 seconds and 17 poses for each
8-second nod direction. All 47 full states, 36 draw-only orientation views and 83 nonblank images
retain the exact groom/calibration and source attestation. The only replaced response is the groom
GLB. No renderer or solver method is patched. Full body and hair triangle replay includes caps
and all root-layer cards; these groups are observations, not exemptions.

| Run | Poses | Max face pairs | Max neck pairs | Max cap pairs | Max root-layer pairs | Max curtain pairs |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Natural | 13 | 7 | 0 | 0 | 349 | 21 |
| Nod + | 17 | 7 | 0 | 26 | 329 | 14 |
| Nod − | 17 | 13 | 4 | 80 | 481 | 47 |

An earlier progress summary incorrectly generalized clear final natural samples to the whole
run. The saved complete reports already contained its middle-run failures; direct replay agrees.
That all-clear statement is withdrawn. These maxima and the evidence JSON come from every
saved result. Neither capture bytes nor the valid intersection instrument changed.

The nod− calibrated-neck residuals are first spans of cards 281, 383 and 396. Their closest
norm-tube witnesses reach the fixed root at fraction zero, with held deficits of 2.50, 1.93 and
5.98 mm. Both root corners have full head-bone weight. The current contact rule activates when
reevaluated at these saved states, but zero root mass and a zero-fraction witness leave no free
endpoint that can resolve the constraint. More iterations cannot fix that particular condition.
The actual crossing-segment UV boxes plus bilinear halo have zero alpha at base/mip1/mip2 and
maximum 0.01624 at mip3. These conservative transparent-root contacts remain in the strict gate.

Separate crossing segments outside the calibrated patch have opaque atlas witnesses. Natural
frame 420 card 212 spans 12–14 crosses upper-body triangles 14338/14339/14343/14348 with base
alpha 1. In nod−, card 281 spans 0–3 and card 396 spans 0–1 have opaque witnesses on excluded
upper-neck triangles,
including different segments of the same first spans. These witnesses persist in CPU box mips1–3.
They are evidence of opaque geometry contact; final camera visibility and occlusion are not proved
by atlas sampling alone. No domain expansion or opaque-envelope solver change is included.

Maximum saved velocity is 2.655 m/s in natural, 1.789 m/s in nod+ and 4.427 m/s in nod−.
The largest nod− spike is card 8/ring 15, whose adjacent spans are already 11.57–16.04 mm clear
of the neck envelope and moving outward in the saved state. The three infeasible-root cards
have smaller downstream spikes, peaking at 1.468 m/s; all inspected fixed-root velocities are zero.
Saved-state correlation does not identify per-substep impulses. Maximum observed segment error
is 0.7993 mm. No arbitrary tuning or performance claim follows from this diagnostic.

The complete record is `captures/bob01-g025-avatar-motion-2026-09-09`: 391 manifest entries,
exact canonical/served sources, all raw states/images, complete triangle tables, opacity witnesses
and focused constraint/velocity reports. The original shipping g025 GLB remains unchanged.
