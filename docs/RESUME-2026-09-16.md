# Sugata resumed — collar-opening diagnosis, September 16, 2026

Rob explicitly authorized development in this new session after the September 13 pause.
The older recurring automation and its separate Goal were left untouched. No reset credits,
model changes, pushes or publication were used. This is a bounded diagnostic checkpoint;
the full AAA-quality, humanlike embodiment brief remains the target.

## Recoverable starting point

Work ran directly in `/Users/robault/GitHub/sugata-avatar`, on `codex/local-restart`.
The starting tree was clean at `6e88b870d7be7b7a8cf768a943a645122c2c9a52`, with the recorded
origin branch 25 commits behind and none ahead. No fetch or remote change was made.
The other existing worktree (`/Users/robault/.codex/worktrees/affc/sugata-avatar`) remains
detached at `2087931`; it was not used or modified. No applicable `AGENTS.md` was found in
the checkout or its ancestor directories.

Read the [brief](BRIEF.md), [September 13 pause](PAUSED-2026-09-13.md), current evidence here,
and [restart](RESTART-2026-09-08.md)/[overnight](OVERNIGHT-2026-09-08.md) context.
`CHECKPOINT.md` and the older body of `PROGRESS.md` retain historical work; do not restart
their superseded experiments. All 47 files in the pause archive were rehashed successfully.
The original ignored evidence and scratch assets remain in place.

## Finding and visual evidence

The dark side/back neckline slits in the exact frozen `casual-collar-v2.glb` are views of
**culled back faces of the single-sided tee**. Giving the same geometry visible back faces
fills the slits in the actual WebGPU rendering. This is evidence for a collar-interior
correction, not a reason to change body masks or shared foundations.

Candidate SHA-256: `ca65319add5a41147df9d894aebe6441a9ab6f5131ee639dd49c3bef74861e5e`.
The candidate remains **unpromoted and byte-identical**. The control is a runtime-only
`FrontSide → DoubleSide` material change; no new shipping asset was created.

| Same candidate and native 1.5 s pose | Diagnostic with visible back faces |
| --- | --- |
| ![Ordinary candidate](../captures/collar-opening-2026-09-16/gpu-v3/ordinary-converged.png) | ![Two-sided diagnostic](../captures/collar-opening-2026-09-16/gpu-v3/double-sided.png) |

First inspected both preserved ordinary and magenta-foundation-tag images. Their dark
pixel sets in the two selected neckline windows have zero RGB response to the tag.
The new capture reproduces the saved bone/morph arrays, camera, hair position/velocity/
vertex hashes and simulation clock. Its ordinary image differs from the saved image at
702 of 692,172 pixels, maximum 28/255; physical reproduction is not pixel identity.

The probe casts 1,634 pixel-center rays in two declared windows, using the committed
skinned pose and unjittered camera. Of those, 162 are dark in the settled ordinary image
and first hit a back-facing tee triangle with culling disabled:

| Window | Selected dark samples | Original tee triangle IDs | Raw vertex IDs |
| --- | ---: | --- | --- |
| Left | 30 | 1432 | 512, 686, 832 |
| Right | 132 | 597; 598; 599 | 82,375,268; 82,268,371; 82,371,261 |

Every selected ray misses all drawn body/garment surfaces under ordinary culling, and still
misses them when the complete original body index is temporarily restored for the CPU
query. These rays therefore do not support the previously refuted missing-body explanation.
They are **not** the six triangles implicated in the prior candidate's contact audit.
GPU-deformed hair is excluded from CPU picking; the GPU material control supplies the
visual corroboration.

At those selected samples the two-sided control increases at least one RGB channel by
140/255 (left) or 111/255 (right). Serial capture phases introduce nuisance variation:
the restored ordinary control differs by up to 19/255 and 28/255, respectively, despite
identical physical state. The serial magenta control also varies by up to 15/255 and
25/255, so do not call it pixel-identical or attribute all of that difference to the bra.
The saved matched-phase tag pair, ray ownership, large side-control response, and return
of the slits after restoring culling jointly establish the narrower diagnosis.

Reviewed the actual ordinary candidate at yaw −45°, +45°, 135°, and 180° as well. Obliques
expose the open interior and angular collar contour; rear views do not make the garment
finished. The control fills the front-view slits but retains a thin, angular neckline.
Neither collar nor overall avatar is visually accepted as AAA by this work.

## Checks and limits

- A separate byte/geometry audit confirms 96 changed positions, 126 changed normals,
  maximum displacement 18.402916 mm, 180 incident triangles, minimum original-to-candidate
  face-normal dot 0.612567 and minimum area ratio 0.673248. No positional seam disagreement.
- Only position/normal bytes and their accessor bounds differ from the accepted garment;
  the exact export matches the frozen solver recipe. Topology, UVs, joints/weights,
  masks, materials, textures and skeleton remain unchanged. Lowest changed original
  vertex Y is 1.327516 m, preserving lower trouser geometry.
- Nine new GPU images are hash-verified. All five front-view controls retain identical
  physical state. WebGPU adapter reports `isFallbackAdapter: false`; page errors are empty;
  retirement leaves zero tracked renderer resources and no avatar leaked handles.
- All 26 frozen source/asset hashes used by the preview match before and after the run.
  Accepted hair, foundations, trouser/shoe fit and runtime files were not edited.
- Asset invariants are checked; independent solver review is **not complete**. No final
  simultaneous positive-clearance or containment certificate was added. Prior zero-crossing
  CPU results retain their narrower meaning.
- There is no new captured motion sequence, full wardrobe qualification, or exact frozen
  foundation-coverage replay for this hash. Advancing to a held native pose is not motion
  visual qualification. The prior `fd93e0…` coverage result does not qualify `ca6531…`.

## Next bounded step

Prototype a collar interior/lining or an explicitly qualified two-sided garment treatment,
using the four localized triangles as witnesses rather than as the entire repair boundary.
Choose the patch by collar topology, preserve skinning and UV/material semantics, and compare
ordinary/oblique/moving views with the exact frozen candidate and accepted garment. Do not
move geometry or remove more body coverage simply to hide these holes. A local interior
should cover the exposed collar coherently around the opening, not just these sampled pixels.

Before any promotion, finish solver review and simultaneous clearance/containment checks;
capture actual motion for both bobs; verify oblique and wardrobe states; and replay the exact
frozen foundation selection with this candidate. The replay starting point is
`captures/tee-neckline-2026-09-13/independent-calibration/replay.mjs` and its `REVIEW.md`;
retain its original calibration and qualify the new hash instead of adding it to an allowlist.

## Reproduction and preservation

Tracked diagnostic tools are in `tools/figure-pipeline/collar_{candidate_audit,opening_probe,
opening_browser,opening_analyze}.mjs`. From the repository root:

```sh
node tools/figure-pipeline/collar_candidate_audit.mjs /tmp/collar-audit-new.json
node tools/figure-pipeline/collar_opening_probe.mjs /tmp/collar-opening-new
node tools/figure-pipeline/collar_opening_analyze.mjs /tmp/collar-opening-new
```

Use fresh output paths. The GPU probe needs local-server and Chromium launch permission;
its dense CPU ray pass takes several minutes. Its page uses an unwatched local Vite server,
so routine file edits do not refresh the capture. Frozen helpers are loaded from the existing
pause archive, which must remain resident.

Primary local evidence: `captures/collar-opening-2026-09-16/`; tracked
[summary](evidence/collar-opening-2026-09-16.json) and
[manifest](evidence/collar-opening-2026-09-16.manifest.json) bind its files and tools.
The first attempt was blocked by sandbox local-listen permissions. Two further attempts
stopped at signed-zero comparisons with JSON, which normalizes `-0` to `0`; both retired
cleanly. `gpu-v3` completed with numeric/JSON-normalized saved-state comparison. These failed
attempts are retained and excluded from the nine completed-control images.

All three launched browser jobs are terminal and their servers closed. No background job
was created. The archive is local ignored evidence, not an off-machine backup.
