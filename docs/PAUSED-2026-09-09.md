# Sugata paused — September 9, 2026, 06:30 PDT

> Historical pause record. Rob explicitly resumed on September 13; see [current work window](RESUMED-2026-09-13.md).

Rob explicitly requested a pause and identified the next priority: make the testbed pages
cohesive, current, and clear about what works and what remains incomplete. Do not resume
implementation or GPU experiments until he explicitly resumes. The existing overnight
heartbeat `advance-sugata-avatar-visual-quality` is PAUSED, confirmed through the app and its
configuration. Work remains local; nothing was pushed.

Use `/Users/robault/GitHub/sugata-avatar`, branch `codex/local-restart`. Always specify this
working directory. Do not access the old iCloud checkout inherited by the task environment.
Production HEAD before this checkpoint is `19ea5a7`; its working tree was clean.

## What is in the preview

- Original long bob g050 collision correction (`c871ed9`), with further scoped motion proof.
  Rob independently confirmed that the long bob looks fixed. The accepted chin bob is preserved.
  This is a scoped face/nape result, not an all-body/all-bake clearance claim.
- Actual g050 wardrobe lookbook (`7ea6ea6`) with casual/elegant configurations, hair, framing,
  lighting and orbit controls: <http://127.0.0.1:5197/src/showcase.html>.
- PNG image export and separate JSON settings export (`20569f5`), including actual browser
  validation of state preservation and cleanup.
- Foundation coverage masks under the casual/elegant outfits (`82b3e63`), removing conspicuous
  blouse/skirt patches. The four selected mask arrays changed; outer clothing and body geometry
  did not. Sock/collar fit and a small independently witnessed foundation-only sliver remain open.
- Portrait: <http://127.0.0.1:5197/src/portrait.html?hair=bob01>. Existing dev server remains
  available. The old production server on 5198 is stale; do not treat it as current.

The last accepted build compiled all 18 pages. That is an import/build result, not proof that
every historical page or live dependency currently works. The existing HairMaterial suite has
four known failures; no claim of a fully green repository suite is made.

## Promising changes still outside production

**Original-card render history:** the frozen earlier prototype passed a bounded independent
per-pixel velocity oracle, matched natural/nod appearance, teardown and timing qualification.
It visibly reduces moving-hair noise but failed ordinary cached PNG export. See
[the qualified prototype](HAIR-CARD-HISTORY-2026-09-09.md).

The subsequent Stage-owned candidate preserves ordinary cached PNGs, scopes the previous vertex
snapshot to accepted fresh TAAU beauty, and restores native state after a proven shadow callback
failure. Nineteen CPU groups and an initial GPU/three-PNG-click smoke passed. Independent GPU
testing still catches a direct-forward empty shader output error caused by a velocity-only
material MRT on an unnamed target. A proposed narrow original-MRT lease is prepared but
**unapplied and untested**. The current owner is not ready to install. Its paired numerical/motion,
bob02/lifecycle and final cost/build/no-route integration checks have not run.

**Brow/lash coverage:** the matched TAAU pilot gives softer brows and lashes while preserving
the original color, maps, geometry and binary shadow cutoff. Its eight-arm qualification records
actual draw timing, state parity, compiled shadow programs and zero browser errors. See
[face coverage qualification](FACE-CARD-COVERAGE-2026-09-09.md). The new scoped owner has sixteen
CPU transition/lifetime groups passing, including partial allocation failure. Its actual GPU
transition, TRAA, cached PNG and combined integration checks remain pending. The prepared Avatar
patch is based on an older history-candidate Avatar hash; merge its edits into the final candidate
instead of overwriting the newer build-error cleanup fix.

One more cleanup guard remains untested: failure on the first uniform allocation can leave an
entry without `enabled`, which `clearActive()` currently assumes exists. Fix and test this on
resumption before treating the owner as ready.

Live follower rendering and outer-cloth/foundation/sock fit pilots remain unshipped. Rejected
fits introduced contacts or failed to repair the moving defect. Do not repeat them without a
different mechanism and a concrete prediction.

## Testbed coherence and Converse

No testbed/Converse edits were made during this final investigation. The hub currently leads
with historical `alive` acceptance and phase labels. Several old descriptions are stale: for
example, Converse claims to be the only page using `Avatar.create`, despite the newer runtime
portrait and lookbook. Source comments preserve valuable experiments but are not a current
user-facing feature status.

Read-only diagnosis at about 06:28 PDT: LM Studio is listening on local port 1234, and the dev
proxy `http://127.0.0.1:5197/lmstudio/v1/models` successfully returns seven advertised model IDs.
The hardcoded Converse default `qwen/qwen3.6-35b-a3b` is absent. Current entries include
`qwen3.6-35b-a3b-mtp` and `qwen3.6-35b-a3b-mlx`; do not infer that either is loaded or silently
substitute one. No generation request or model load was made. The mismatch is established, but
the specific user-visible Converse failure has not yet been reproduced in a browser.

Converse has no model picker/reconnect UI, only a query parameter. It warms the historical model
automatically and prints technical failure codes. Its source explicitly uses a synthetic 0.985 s
mouth timeline; no real TTS or microphone integration exists. Built pages need a server-side
proxy or appropriate gateway. These are distinct from a renderer boot regression.

On resumption, prioritize a current entry point and page/status inventory: runtime demonstrations,
focused diagnostics, historical experiments, and external-service requirements. Reproduce failures
before marking pages broken or repaired. Add an actionable Converse model/connection flow and
honest audio status. Clothing should be curated as complete outfits with consistent silhouette,
fit, layering, footwear and palette; the two stand-in looks are a starting point, not a mature
wardrobe collection. Keep this work separate from MLX.

## Recovery artifacts

`captures/overnight-pause-2026-09-09/` preserves the candidate sources, unapplied patch, CPU tests,
independent failure reports, GPU smoke and agent checkpoints outside temporary storage. The
tracked [archive record](evidence/overnight-pause-2026-09-09.json) gives the exact file count, byte
count and SHA256 manifest; every copied payload was independently rehashed. Dependency symlinks
are recorded but excluded. These are research snapshots, not new production modules.

The owner directories remain in `/tmp/orbit-card-history-owned`,
`/tmp/continuity-card-history-review` and `/tmp/visual-face-card-coverage-owned` for convenience.
Read their PAUSED/checkpoint records and verify hashes before using them. Do not blindly rerun
preparation scripts: later reviewed fixes are already present in the frozen sources.

All agents were asked to stop launching work and close their test resources. The final root
response confirms completion of that shutdown; the user's dev preview is intentionally retained.
