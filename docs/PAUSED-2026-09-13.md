> **September 16 update:** Rob explicitly authorized development in a new session. See
> [the resumption checkpoint](RESUME-2026-09-16.md). That authorization leaves the older
> recurring automation paused and its separate Goal unchanged. The September 13 record
> below is preserved as historical handoff context.

> **Final stop status:** The separate Goal is now BLOCKED pending Rob's explicit authorization to resume, after repeated automatic continuations encountered the same pause boundary. Scheduled work remains PAUSED. The earlier active-Goal/app-side-Stop note below records the initial stop state; no app action is now needed to stop continuation. The project is unfinished and no development has resumed.

# Explicit user pause — Sunday evening, September 13, 2026

Rob asked to stop soon and call it a night during this pass. He then explicitly requested a macOS notification because the app conversation remains blank. No further development is authorized until a new direct resume request. Any automatic continuation must honor this pause.

Live runtime is still the accepted6c33af5 code/assets. Repository checkpoint before this pass is a2f4a6d. No candidate, source, mask or accepted asset digest changed in this pass. The existing5197preview can remain available; the desktop app was not restarted. The recurring heartbeat advance-sugata-avatar-visual-quality has been updated through the native tool to PAUSED. The separate active Goal cannot be paused through the exposed goal tool; it still reports active, and its Stop control requires app-side action. It is not marked complete because the broader work remains unfinished.

Latest scratch candidate: casual-collar-v2.glb, SHAca65319add5a41147df9d894aebe6441a9ab6f5131ee639dd49c3bef74861e5e. Starts prior unpromoted necklinev2, then applies sampled three-dimensional body clearance using authored and captured native0/4.5s transforms. Neighbor corrections are smoothed with inverse-squared-edge weights.96 positions/126 normals change; maximum18.403mm, min source normal dot.613, min area ratio.673. All lower-trouser positions remain outside the changed region (lowest changed Y1.3275m). No general clothing/simulation API is implied.

First solve-v1 passed its sampled clearance iterations but failed the shape guard at normal dot.193/area ratio.178. The guard's original message 'Folded triangle' overstates this: the minimum dot is positive, so actual inversion was not proved. That failed candidate was not exported or rendered. solve-v2 adds coherent neighboring corrections and passes those shape guards. Its final pre-projection iteration minimum is not a final simultaneous dense clearance certificate; independent solver review remains unfinished.

Root whole-incident triangle checks for the broader96-vertex region give authored skin64→0 and retained bra18→0. CPU displacement replays anchored to recorded source positions give native skin68/72/72→0 and bra26/25/25→0 for each bob at0/1.5/4.5s. The1.5s state is held out of fitting. Source/prior replay errors are about1.2e-7m or less. These are CPU predictions using actual captured rigs, not new GPU motion captures; zero shell crossings does not establish positive clearance or exclude containment.

The already-running two-arm GPU held-pose comparison completed and retired cleanly before pausing: ordinary trial and magenta foundation tag, native1.5s. Root viewed only the ordinary trial through an original-size JPEG copy. It covers the previous gray spots, but dark slit-like openings are visible at the back/side neckline. Their cause is not isolated; do not assume the old refuted body-mask explanation is now proved, and do not promote the candidate. The tag view is retained but was not independently reviewed before the stop request.

The critic saved localization of the prior v2 contacts and two alternatives. Six tee triangles contain the actual prior-candidate skin crossings; new pairs occur on1489 and1512. The smallest surrounding neighborhood contains24 welded/30 raw vertices and58 incident triangles. The critic explicitly left the new solver invariant/final simultaneous-clearance review unfinished and excluded a faulty aggregate in its own scratch output. Its frozen manifest is9e6faa376034b36559039784016827f2c3859fc32d4475ea4db7d75a4eb523b6 (five files,258,295bytes).

On an explicit future resume, first inspect the preserved ordinary/tag views and localize the new dark openings before changing geometry or body coverage. Then finish independent solver/asset invariant review, actual GPU motion/oblique/wardrobe checks, and exact frozen foundation-coverage replay for this new hash. Preserve accepted hair, foundations and trouser/shoe fit. Do not treat the previous candidate's calibration pass as evidence for this candidate. Preserve the AAA/humanlike embodiment ambition; the pause changes timing, not scope.

Root process handles28995 (solver),20878 (CPU replay/contact checks),16212 (GPU preview) are terminal and polled. The critic has finished. No new GPU, encoding, build or integration was launched after Rob's stop request. Saving and notification are the only closing actions.

Archive: 47 files / 31981890 bytes, copied and reread/hash-verified. ManifestSHA256 9798a68896d62f1ad546e6f0c094fda5a6fc636bf039032b727acf0ec282a71c. Captures are local ignored evidence, not an off-machine backup.
