# Sugata — September 24 afternoon and evening

Robert resumed this project with the existing full Git-operations and test authorization.
The work starts at `9c9c647` on `codex/resolve-eleven-gates`, draft PR #3. The intended window
ends at **10 p.m. America/Los_Angeles on September 24** (September 25, 05:00 UTC).
The existing `advance-sugata-avatar-visual-quality` heartbeat now returns to this task every
45 minutes through that window. It replaces the expired September 13 schedule and its scope.
Pause it after the evening checkpoint; do not extend the window automatically.

## First checkpoint: correct visibility before fitting the cap

The asset verifier counted scalp samples hidden behind the body's own ear as visible bare
skin. It tested the sample's facing normal and the hair cutout, but never tested intervening
body triangles. `skinVisibleFrom` now checks both. Its arithmetic lives in `hair_geometry.mjs`
so synthetic surfaces can test the actual operator used by the verifier.

This changes only the view-dependent visibility measurement. Normal-ray scalp coverage,
the largest normal-ray bald patch, clearance, gathering, depth complexity and all acceptance
thresholds remain unchanged. No runtime code or shipped asset changed.

| Shipped g050 groom | Previous worst visible patch | With body occlusion | Ceiling |
| --- | ---: | ---: | ---: |
| crop01 | 158.4 mm² | 105.9 mm² | 60 mm² |
| pixie01 | 182.5 mm² | 129.9 mm² | 60 mm² |
| quiff01 | 770.3 mm² | 770.3 mm² | 60 mm² |

All three still fail. The fix removes an overcount; it does not qualify these grooms.

Validation on the modified tree:

- Geometry selftest: **43/43**, including 13 visibility cases for an open sightline, self-hit
  precision, partial occlusion, reversed winding, close and distant blockers, transparent hair,
  facing direction and rotation. Removing body occlusion deliberately fails five assertions.
- Verifier selftests pass. The full asset verifier still reports **20 problems across 43 files**,
  with the same failing clauses as the prior checkpoint.
- Production and Pages builds pass. The 126-gate runtime suite was not repeated for this
  offline measurement change; its September 22 integration evidence remains explicitly historical.
- Real WebGPU still captures of the crop and cap candidates completed. They inspect rest shapes
  on `hair.html`, not the full runtime's motion or material acceptance.

Raw logs and measurements are in [the evidence directory](evidence/hair-2026-09-24/).
The four declared red gates remain HairMaterial, GLB verification, opacity and tips.

## Cap experiment: useful direction, not a promoted asset

The baseline Blender rebuild reproduces the shipped crop geometry fingerprint exactly:
`bfd90bc5647502b42040e5221815d3aa2f0b5ab1f562791224ef310d82e71760`.
The experiment extends only the cap's boundary onto adjacent body faces, excluding ears and
the moving facial region. One face ring takes the cap from 282 to 316 faces and cuts the crop's
normal-ray bald patch from **109.7 to 50.6 mm²**, still above the 50 mm² ceiling. Two rings reach
338 faces without further closing that gap. The analogous pixie two-ring patch is 55.8 mm².
Adding more of the same cap geometry is therefore not the next experiment.

The comparison against fully opaque cards also remains red, establishing that the gaps are
partly geometric. Body-occlusion correction alone does not remove them. The next diagnosis
should distinguish the cap's offset edge from absent scalp coverage around the ear and the
quiff's exposed front hairline; do not cover the ear or moving brow just to lower a number.

Every card's positions, normals, both UV channels, weights, joints and local triangle indices
match the corresponding shipped card exactly in these candidates; see `card-preservation.json`.
Caps and their vertex counts do change. No candidate has motion qualification and none was
copied into `assets/`. The two preserved side plates use the page's groom-derived framing;
they are visual inspection evidence, not a pixel-aligned difference measurement.

The unqualified recipe is saved as `evidence/hair-2026-09-24/cap-candidate.py`. Invoke it with
Blender and explicit separate `--output` and `--hair-dir` paths under `tmp/`, the normal
`build_figure.py` arguments, and `CAP_RINGS=1` or `2`. Source, logs and candidate files from
this run are under `tmp/hair-sep24/`; the tracked recipe and evidence survive cleanup there.

## Continuation environment

Blender 5.2.2 was restored from the same verified cached DMG recorded in the September 22
checkpoint. It is mounted read-only at `tmp/hair-sep24/blender-volume`, with an isolated
`BLENDER_USER_RESOURCES` at `tmp/hair-sep24/blender-profile`. Both are inside this checkout.
No system application or normal Blender profile was changed. Reuse this environment while it
exists; detach the volume at the evening stopping point.

The first-checkpoint tests and capture processes have finished, and its Vite server is stopped.
No asset candidate is awaiting installation. Before the next bite, inspect Git and existing
jobs, read this note and the September 22 repair checkpoint, and continue from the actual
branch. Preserve the unrelated detached worktree and the contact-calibrated bob geometry.

## Second checkpoint: independent opacity controls

The 15:01 Pacific continuation starts from `132e398`, with a clean tree and no active jobs.
An experiment added a rest-position phase to the stochastic transparency threshold, testing
whether coincident decisions across cards were the cause of the transparent side curtain.
It reduced mass transmission from 0.0650 to 0.0503 but **worsened curtain transmission from
0.5439 to 0.5842**. It was rejected before broader qualification. The exact patch (in `rejected-rest-phase.json`) and both portrait
plates are preserved in `evidence/hair-2026-09-24/transparency/`; runtime code is restored.
This result rejects that candidate, not every possible way of decorrelating coverage.

The accepted change fixes the opacity probe's remaining tautological checks. L2/L3 previously
divided the reference step by itself, reporting exactly 1.0000 regardless of capture quality.
They now compare that hidden-hair reference with two fresh captures with the hair physically
detached from its parent. Parent, child order and visibility are restored afterward. A two-sided
0.97–1.03 interval catches both residual hair in the control and a failed reference hide;
appearance limits remain unchanged.

| Probe condition | Portrait footprint L2 | Portrait curtain L3 | Rear footprint L2 |
| --- | ---: | ---: | ---: |
| Healthy | 0.9951 | 0.9923 | 0.9913 |
| Hair left in detached control (`hiddenstuck`) | 0.1662, rejected | 0.5392, rejected | 0.0876, rejected |
| Reference hide ignored (`hidebroken`) | 6.7515, rejected | 2.3254, rejected | 9.3445, rejected |

All rows come from actual WebGPU runs with the normal 24-step captures. The shifted-mask control
still fails the independent outside-footprint L1 check at 0.9643. L2/L3 deliberately make no claim
to validate mask alignment: these ratios should also agree over background. Their descriptions
now state that limit. `control-audit.json` records the checked results and evidence hashes.

The healthy run still fails **only C4**, at the unchanged 0.5439 versus 0.35. It is not a newly
green hair gate. Runtime and shipped assets remain unchanged, so there was no new full-suite or
build rerun for this probe-only repair. Syntax validation, request-ledger and quoted-number
checks supplement the healthy and three planted-defect GPU runs.

Next: continue the geometry/atlas investigation with these corrected measurements. The live
`alive.html` probe does not use Avatar's root-feather option, so changing Avatar's `fadeLength`
would not test this C4 failure. Keep that distinction explicit when comparing the legacy testbed
with the public Avatar runtime. No shader change or cap candidate is pending promotion.

All second-checkpoint GPU and CPU checks have finished, and their Vite servers have stopped.
The Blender mount is still available for the authorized afternoon/evening window; no Blender
process is running. The next wake should inspect current jobs and Git before continuing.

## Third checkpoint: cap boundary experiments and synchronized capture skinning

The 15:46 Pacific continuation starts from `7f77a6c`, clean and idle. Ray witnesses confirm that
the remaining side-view samples in the one-ring cap experiment do not intersect a cap triangle;
transparent cap texels are not the cause. A bounded tangential expansion of the cap boundary
was therefore tested. Cards and their full attribute payloads stay identical to the shipped crop.

| crop01 g050 geometry | Normal-ray bald patch, ceiling 50 mm² | Worst visible patch, ceiling 60 mm² |
| --- | ---: | ---: |
| Shipped | 109.7 | 105.9, side |
| One extra face ring, remeasured | 50.6 | 78.3, three-quarter |
| One ring plus 1 mm boundary expansion | 47.0 | 65.9, three-quarter |
| One ring plus 2 mm boundary expansion | 57.3 | 60.4, three-quarter |

These all use the corrected body-occlusion verifier. The earlier raw cap logs predate that
correction; their 97.6 mm² side-view result is not the current worst-view measurement.
Both new candidates retain 3.501 mm minimum vertex clearance. The 1 mm candidate closes one
coverage clause but still fails oblique coverage, gathering and coherence. The 2 mm candidate
reopens the normal-ray patch and is rejected. Its interaction with clearance correction is a
hypothesis to investigate, not a proven sole cause. **Neither candidate is promoted.** The
underlying cards still look like separate strips rather than coherent locks.

Five ordinary views and a four-second, 240-step WebGPU shake were captured for the shipped crop
and the 1 mm candidate. At frames 1, 60, 120, 180 and 240, every GPU card centre and velocity is
identical between them and finite. This is a comparison on `hair.html` with its plain PBR material;
it does not qualify the public Avatar material, facial-expression contact or all-motion appearance.
The recipe, hashes, comparisons and raw verifier reports are in
[`evidence/hair-2026-09-24/cap-edge/`](evidence/hair-2026-09-24/cap-edge/).

### Accepted repair: the body and hair now share a capture frame

The motion comparison exposed a real capture defect: the camera, CPU head matrices and GPU hair
centres agreed, while the rendered body did not. In one saved first-frame pair, 9,755 of 9,800
pixels in an unchanged face rectangle differed. Awaiting the initial render alone fixed that
first frame but did not fix later motion frames.

Three's skinning node caches a skeleton upload by renderer frame ID. `hair.html` advanced its
simulation on demand while leaving that renderer ID on the wall-clock rAF loop, so several rapid
steps could reuse the body's old pose. Capture mode now stops that internal loop, advances the
renderer clock for each draw and pins its time to the simulation. Capture controls are also
published after the initial draw finishes. Reset rewinds simulation time while keeping renderer
frame IDs monotonic, so it cannot reuse a stale skeleton upload. Ordinary animation keeps its
normal frame loop.

The corrected comparison has **zero changed face pixels** at all five saved matching states;
comparing different poses changes 9,797 of the same 9,800 pixels. A new actual-WebGPU regression,
`hair-capture-clock.selftest.mjs`, is automatically discovered by the existing runner. It checks
three moving poses across fast and delayed loads/steps, renderer counters and the actual body
bone-upload array. Removing the frame tick leaves stale bone matrices and fails; leaving rAF
running fails the counter oracle. These controls do not rely on the cap candidate.

Validation on the modified tree:

- New capture-clock regression: **7/7 groups**, including both source mutations.
- HairDynamics: **24/24 with `--quick`**; its full rejection suite was not repeated.
- Request ledger: **27/27**. Quoted-number checks: **25/25**. Pages build and syntax checks pass.
- Corrected candidate capture: exact card dynamics, camera and head matrices, with unchanged-face
  pixel equality and a mismatched-pose negative control. Earlier capture-helper errors were
  corrected before accepting this result; `capture-failure.json` preserves the before evidence.

No core runtime or shipped asset changed. The four declared red gates remain open, and there
was no new full-suite run. All jobs have finished; the Blender mount remains available within
the authorized window. Next, diagnose the short groom's card-guide alignment and ribbon frames
before increasing clump strength or cap coverage again. `grow_layer`, `draw_into_lock` and
`ribbon_of` in `hair_cards.py` are the relevant existing mechanisms; continuous lock guides
already exist, so the next step should test how cards follow them rather than assume they do not.
## Fourth checkpoint: clearance folds and ribbon width

The 16:31 Pacific continuation starts from clean `002ca47`, with no running jobs. This pass
instruments the existing crop01/g050 guide, lock blend, clearance correction, ribbon frame and
final vertex clamp. It finds a concrete generator defect but **does not promote a correction**:
the isolated candidates still fail geometry and visual quality. Accepted assets, calibrated bob
geometry, runtime code and the production generator remain unchanged.

The baseline rebuild reproduces every shipped card attribute and local triangle index. Of its
384 cards, 122 have a pre-clamp arc shorter than their full root width, and 291 have an aspect
ratio below 2. But broad cards are not the only problem. The clearance pass after lock blending
creates most of the sharp reversals:

| Diagnostic | Shipping recipe | Outside-gradient candidate |
| --- | ---: | ---: |
| Guides with a >90° turn before lock blend | 7 | 7 |
| After blend, before clearance | 4 | 4 |
| After clearance | 61 | 5 |
| Ribbon frames turning >90° between adjacent rings | 54 | 4 |
| Guide roots moved more than 1 mm by clearance | 314 | 3 |

The guide-turn diagnostic requires both segments to be at least 0.1 mm, excluding near-zero
Float32 steps; it is not an acceptance threshold. Unfiltered guide/ribbon-midpoint turn counts
were 79/87, which is why the earlier live update quoted 87. The frame-flip count is unchanged
by the final corner clamp. Raw arrays and the summary make these definitions reproducible.

`draw_into_lock` projects a point to `closest_location + interpolated_normal * standoff`.
A shading normal need not follow the closest-point displacement. On an analytic plane with a
45° smooth normal, three production passes move a point **21.213 mm sideways** and still leave
it only **7.071 mm** off a requested 10 mm offset. The isolated candidate uses the actual
closest-point displacement direction for outside points: it reaches 10 mm without lateral
movement. Aligned outside and inside controls pass in both versions. This demonstrates the
mechanism; it does not establish a complete solution for nonconvex body geometry.

The candidate changes only that post-blend clearance direction. A separate width arm limits
six outer layers' full width to half their guide arc, leaving root/underlayer coverage cards
unchanged. A fourth arm combines them. All use the original cap and existing thresholds:

| Arm | Gather index ≤0.81 | Coherent relief / floor, mm | Normal bald patch ≤50 mm² | Visible patch ≤60 mm² | Minimum clearance ≥3 mm |
| --- | ---: | ---: | ---: | ---: | ---: |
| Shipping | 0.919 | 1.18 / 2.64 | 109.7 | 105.9 | 3.501 |
| Clearance direction | 0.931 | 0.00 / 2.68 | 120.0 | 116.6 | 3.508 |
| Width only | 0.914 | 1.22 / 2.49 | 131.8 | 151.5 | 3.501 |
| Both | 0.929 | 0.36 / 2.44 | 135.4 | 146.0 | 3.508 |

Each asset verifier runs all 22 hair clauses and reports the same four failing clauses. Fewer
folds make the broad, flat plates more apparent; narrowing the outer cards leaves this appearance
and worsens coverage. The combined front, side, back and top plates remain visibly unsuitable.
**All three candidates are rejected for promotion.** Fewer folds alone is not visual acceptance.

Actual WebGPU checks ran serially for each candidate against shipping: five still views and
41 samples across 240 fixed steps of a four-second shake. Sampled positions and velocities are
finite. Across all arms, worst segment error is below 0.000113 mm and fitted-skull penetration
below 0.000069 mm, within the existing HairDynamics limits of 0.01 and 0.05 mm respectively.
These are fitted-sphere/PBR checks, not full-body contact or public Avatar material qualification.

The cap-only experiment's fixed face rectangle proved unsuitable for changed cards: moving hair
changed 162 pixels in it. The updated isolated capture helper pins camera framing and compares
the entire body rendered with the actual hair meshes detached. All five paired body images are
pixel-identical; different-pose control images differ. The initial failed control is preserved.

The recipe, analytic controls, source/asset hashes, per-layer summary, verifier logs, GPU reports
and native comparison plates are in [ribbon-shape evidence](evidence/hair-2026-09-24/ribbon-shape/).
No runtime or asset change warrants a full-suite repeat. This checkpoint leaves the four declared
red gates unchanged. Request-ledger checks pass 27/27 and quoted-number checks pass 25/25;
the capture script's syntax and `git diff --check` also pass. Experimental builds and capture jobs have finished; their Vite servers and
browsers are closed. The isolated Blender mount remains available for the authorized window.

**Next bounded step:** attribute the visible overlapping plates to their actual generator layers
using diagnostic colors or masks before changing more widths or card counts. The width experiment
intentionally retained broad root/underlayer cards; which layers dominate the objectionable
surfaces is not yet measured. Use that evidence to co-design coverage and lock shape. Preserve
this clearance diagnosis, but do not install its candidate alone or repeat blind clump/cap sweeps.
The heartbeat remains active until the existing 22:00 Pacific deadline.
