# Tee neckline fit study — 2026-09-13

**The gray patches are exposed foundation surfaces. A narrower trial neckline covers them, but the trial is not installed because it introduces some skin intersections.** The accepted runtime remains commit6c33af5. This is a checkpoint in the continuing Sunday work window, not a pause or a completed clothing system.

The local comparison is `captures/tee-neckline-2026-09-13/gallery/review.html`. Its current/trial labels distinguish saved study images from the live avatar. `NEXT-FIT.md` in that archive contains the concrete continuation.

## Cause and correction

Four fresh, matched native1.5-second attention views isolate the current outfit, a magenta foundation, a hidden foundation, and full body-index restoration. Tagging the bra colors both patches; hiding it removes both. Restoring all body triangles leaves them. The root initially misread that final image and explicitly corrected the interpretation after independent review. The right patch rectangle is pixel-identical; the left differs by one pixel at one code value. Full-body drawRange80,268, versus51,036 current, proves the restoration persisted. Body-mask changes are not a repair for these spots.

Eighteen camera rays selected from the paired magenta response all hit bra first with either body-index variant. Three also hit tee behind the bra. The critic independently reconstructs78 recorded triangle intersections within5.6e-16m. This is actual posed CPU perspective evidence, not GPU pixel picking. A separate authored front grid has140 visible-bra witnesses:107 through the opening and33 ahead of cloth. The two spaces remain distinct.

## Trial and limits

The first CPU experiment applied projected depth constraints too broadly across wrapping shoulders and fixed neighboring points. It demanded178mm lift and folded faces; displacement/orientation guards rejected it before rendering. The preserved failure is not a production defect.

Trialv2 narrows the front opening20percent, raises it5mm, and tapers over40mm of mesh distance. Bounded front point fits set depth.58 positions and90 normals change, across111 incident triangles; maximum displacement18.855mm. Exact positional twins move together. Vertex order, UVs, indices, skin weights, materials and the accepted lower-trouser fit remain. The scratch GLB SHA is `fd93e0d90a4b723ad84827ffd2a947b74f0d7b9bf4a4606b94299433a38cacac`.

In the independently reviewed held view, both gray interruptions disappear. Ordinary and magenta-tag trial images differ by one channel of one pixel at1/255, with no qualifying magenta response. All18 selected rays now hit tee2.830–4.078mm before bra. The neckline's existing angular construction remains; a sewn, rounded collar is not claimed.

Four additional runs cover both bobs with current/trial fit, native4.5-second attention at60Hz,544 PNGs sampled at30Hz, and three final oblique/front views.659 source/helper/artifact hashes and18 paired action/bone/morph/hair/card/camera/diagnostic states match. Four encoded clips contain136 frames each and run4.533 seconds, including the initial frame. The gallery passes12 comparisons, both videos advancing for each bob, and390px layout. These checks are not full-video perceptual acceptance.

## Why it stays a prototype

Triangle-contact checks find fewer total skin pairs in the actual sampled poses but some new pairs. Over the111 affected-incident tee triangles:

| Space / target | Current | Trial | Newly appearing pairs |
| --- | ---: | ---: | ---: |
| Authored / drawn skin |21|26|11|
| Authored / retained bra |18|102|102|
| Native0s / drawn skin, each bob |26|22|4|
| Native1.5s and4.5s / drawn skin, each bob |26|22|5|
| Native0s / bra, each bob |26|0|0|
| Native1.5s and4.5s / bra, each bob |25|0|0|

The critic verifies12 saved native geometry captures and all28 newly appearing skin-pair witnesses across the six actual poses. Intersection segments range17.8µm–7.927mm; maximum plane residual is1.98e-16m. These are geometric crossings, not penetration depth or proof of visibility at a particular pixel. Triangle IDs use each saved full/drawn index array. The raw authored102 bra pairs must not be presented as actual-rendered102 crossings.

All23,310 frozen additional foundation-removal footprints still pass on the full trial geometry in authored and historical0/4s states. Historical source pose reconstruction agrees within1.192e-7m. Excluding every changed triangle fails2,765 bra and2,773 vest authored footprints, so the proof depends on the new geometry. The calibration's footprint/depth slab contract does not prove positive clearance and does not approve the new skin contacts. No accepted asset digest was changed.

The next bounded approach is to fit the collar's three-dimensional wrap around the measured neck surfaces, preserving twins and checking complete incident triangles. The distinct alternative is tailoring the shared bra's inner strap path and stand-off under the original tee, with foundation-alone and other-outfit checks. Indiscriminate additional masks are unjustified where no cloth covers the opening. Future collar tessellation would also require requalifying the vertex-index material recipe.

All11 valid Avatar runs retire with zero native memory/no leaked handles. The earlier failed bare-module ray harness also retires cleanly; moving its import into a Vite-served module resolves that harness failure. The first gallery run reports an unlocated404; after adding an inline favicon, the full rerun has no resource or console errors. Both failed probes and pre-favicon HTML are retained. A mobile review caught unequal image alignment from a wrapped caption; the shorter trial label and a fresh layout check align both image tops. No runtime build/tests are repeated for this documentation-only checkpoint.

Ignored captures are local evidence, not an off-machine backup. Source hashes, original PNGs, diagnostic controls, trial code/assets, view copies, independent reviews and the continuation note are retained. The existing preview stays on the accepted runtime, with no desktop-app restart, push or reset redemption.

Archive: 806 files / 390650139 bytes, copied and reread with SHA-256 verification. Tracked manifest: `docs/evidence/tee-neckline-2026-09-13.manifest.json`, SHA256 `cc3603e31469ce5eeebec913704893988caf90de0312c57eb2d32cdbc9ffcb8c`.
